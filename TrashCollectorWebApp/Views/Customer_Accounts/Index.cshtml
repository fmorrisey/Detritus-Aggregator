@model IEnumerable<TrashCollectorWebApp.Models.Customer_Accounts>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Customer_ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Balance)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Customer_ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Balance)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Account_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Account_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Account_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
